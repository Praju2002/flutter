const { Maze, Cell } = require('./mazeUtils');

function generateKruskalMaze(rows, cols) {
  // ... (your existing Kruskal maze generation code)
}

function generatePrimMaze(rows, cols) {
  // ... (your existing Prim maze generation code)
}

module.exports = { generateKruskalMaze, generatePrimMaze };
